FRONTEND RESPONSIBILITIES

The frontend is responsible for the user interface.

In this project:
- The frontend is a simple HTML page
- It displays a button to the user
- It sends a request to the backend when clicked
- It receives data from the backend and displays it on screen

This shows that the frontend handles:
- User interaction
- Displaying data
- Sending requests to backend APIs
